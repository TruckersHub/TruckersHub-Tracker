"use strict";
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.lib = void 0;
const fs_1 = require("fs");
const os_1 = require("os");
const path_1 = require("path");
function checkPath(path) {
    const steamPath = (0, path_1.join)(...path);
    if (!(0, fs_1.existsSync)(steamPath))
        return false;
    return steamPath;
}
function getSteamPath() {
    return __awaiter(this, void 0, void 0, function* () {
        // As far as I am aware MacOS only installs steam to ~/Library/Application Support/Steam
        const paths = [
            [(0, os_1.homedir)(), ".steam", "root"],
            [(0, os_1.homedir)(), "local", "share", "Steam"],
        ];
        for (const path of paths) {
            const res = checkPath(path);
            if (!!res)
                return res;
        }
        throw new Error("linux: Unable to find steam install path");
    });
}
exports.lib = {
    getSteamPath,
};
